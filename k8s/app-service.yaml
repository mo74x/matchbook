apiVersion: v1
kind: Service
metadata:
  name: matchbook-app
  namespace: matchbook
  labels:
    app.kubernetes.io/name: matchbook
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      name: http
  selector:
    app: matchbook-app
